import { NextPage } from "next";
import Navbar from "../components/navbar";

const page: NextPage = () => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default page;
